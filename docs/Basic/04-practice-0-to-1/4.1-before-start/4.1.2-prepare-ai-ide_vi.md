---
title: "4.1.2 Chuẩn bị AI IDE của bạn"
order: 3
---


![04-practice-0-to-1_4.1-before-start_4.1.2-prepare-ai-ide.png](../../../public/images/Basic/04-practice-0-to-1_4.1-before-start_4.1.2-prepare-ai-ide.png)
# 4.1.2 Chuẩn bị AI IDE của bạn

> **Mục tiêu phần này**: Dùng 5 phút để xác nhận AI IDE của bạn đã sẵn sàng

## Xác nhận công cụ đã cài đặt

Nếu bạn đã học theo Chương 1, chắc hẳn bạn đã chọn và cài đặt một AI IDE.

Chưa cài đặt? Vui lòng xem lại [1.3 Hướng dẫn chọn công cụ](../../01-awakening/1.3-tools-guide/), cài đặt xong rồi hãy tiếp tục.

::: warning Quan trọng
Chương này sử dụng AI IDE cài trên máy local để phát triển. Hãy đảm bảo máy tính của bạn đã cài đặt công cụ.
:::

## Làm quen với bố cục giao diện AI IDE

Bất kể bạn dùng AI IDE nào, chúng đều có cấu trúc giao diện tương tự:

```
┌────────────────────────────────────────────────────────────┐
│                        AI IDE Bố cục chung                   │
│                                                            │
├──────────────┬─────────────────────┬───────────────────────┤
│              │                     │                       │
│   Khu vực    │      Khu vực        │       Khu vực         │
│    File      │        Code         │      Xem trước        │
│ (File dự án) │ (Code AI tạo ra)    │ (Hiệu quả thực tế)    │
│              │                     │                       │
├──────────────┴─────────────────────┴───────────────────────┤
│                        Khu vực                             │
│                       Đối thoại                            │
│               (Nơi bạn nhập Prompt)                         │
└────────────────────────────────────────────────────────────┘
```

### Khu vực Đối thoại: Nơi bạn giao tiếp với AI

Đây là khu vực quan trọng nhất. Bạn nhập Prompt (còn gọi là chỉ lệnh, mô tả nhu cầu) tại đây, AI sẽ hiểu ý định của bạn và sinh ra code.

**Tìm nó**: Thường ở dưới đáy hoặc bên cạnh giao diện, có một ô nhập liệu, có thể ghi chữ "Ask AI", "Chat", "Composer" v.v.

### Khu vực Code: Nơi hiển thị code AI sinh ra

Code do AI sinh ra sẽ hiển thị ở đây. Thông thường bạn không cần chỉnh sửa trực tiếp những code này, nhưng có thể xem.

**Tìm nó**: Thường ở giữa giao diện, hiển thị nội dung file như `index.html`, `app.js`.

### Khu vực File: Nơi quản lý file dự án

Ở đây hiển thị tất cả file và thư mục trong dự án. Khi quá trình phát triển diễn ra, bạn sẽ thấy ngày càng nhiều file xuất hiện ở đây.

**Tìm nó**: Thường ở bên trái giao diện, là một danh sách file dạng cây.

### Khu vực Xem trước: Nơi nhìn thấy hiệu quả

Đây là khu vực mang lại cảm giác thành tựu nhất. Trang web của bạn sẽ hiển thị thời gian thực ở đây.

**Tìm nó**: Thường ở bên phải giao diện, có thể cần nhấn nút "Preview", "Run" hoặc tương tự mới xuất hiện. Một số AI IDE cũng hỗ trợ mở xem trước trên trình duyệt.

## Tạo thư mục dự án

Trước khi bắt đầu, kiến nghị bạn tạo một thư mục dự án chuyên dụng trên máy tính:

1. Mở thư mục "Documents" hoặc "Desktop"
2. Tạo một thư mục mới, đặt tên là `vibe-coding-projects`
3. Trong đó tạo thêm một thư mục con `todo-list` (Dự án danh sách việc cần làm)

Cấu trúc thư mục như sau:

```
Documents/
└── vibe-coding-projects/
    └── todo-list/        ← Dự án chương này đặt ở đây
```

::: info 💡 Tại sao làm như vậy
Đặt dự án ở vị trí cố định, thuận tiện cho việc tìm kiếm sau này. Các file AI IDE sinh ra đều sẽ được lưu trong thư mục này.
:::

## Mở thư mục dự án trong AI IDE

1. Mở AI IDE của bạn
2. Tìm tùy chọn "Open Folder" hoặc "Mở thư mục"
3. Chọn thư mục `todo-list` vừa tạo
4. Xác nhận mở

Bây giờ, AI IDE của bạn sẽ hiển thị một dự án trống.

## Xác minh công cụ hoạt động bình thường: Một bài test đơn giản

Nhập Prompt sau vào khu vực đối thoại, kiểm tra xem công cụ có hoạt động bình thường không:

```
Hãy giúp tôi tạo một trang web đơn giản, hiển thị câu "Hello, Vibe Coding!", chữ căn giữa, dùng màu xanh dương.
```

### Kết quả mong đợi

Bạn sẽ thấy:
1. AI bắt đầu sinh code (thường là một file `index.html`)
2. Khu vực Code hiển thị code HTML đã sinh
3. Khu vực File xuất hiện file mới
4. Khu vực Xem trước hiển thị một trang web, trên đó có dòng chữ "Hello, Vibe Coding!" màu xanh dương căn giữa

### Nếu mọi thứ bình thường

Chúc mừng! Công cụ của bạn đã sẵn sàng. Có thể nhảy thẳng đến [4.1.4 Danh sách kiểm tra cuối cùng](./4.1.4-checklist.md).

### Nếu gặp vấn đề

| Vấn đề | Nguyên nhân có thể | Cách giải quyết |
|------|---------|---------|
| Khu vực đối thoại không phản hồi | Vấn đề mạng hoặc chưa đăng nhập | Kiểm tra kết nối mạng, xác nhận đã đăng nhập tài khoản |
| AI trả lời rất chậm | Độ trễ mạng | Kiên nhẫn chờ, hoặc kiểm tra tình trạng mạng |
| Code sinh ra rồi nhưng không thấy xem trước | Xem trước chưa bật | Tìm nút "Preview" hoặc "Run", hoặc mở file HTML đã sinh bằng trình duyệt |
| Hết hạn mức Prompt | Hạn mức miễn phí đã hết | Xem có thể mua thêm hạn mức không, hoặc chờ làm mới hạn mức tháng |
| Giao diện toàn tiếng Anh | Cài đặt ngôn ngữ | Không ảnh hưởng sử dụng, AI có thể hiểu Prompt tiếng Việt |

## Về hạn mức miễn phí

Đa số AI IDE đều cung cấp hạn mức miễn phí hoặc thời gian dùng thử. Đối với dự án danh sách việc cần làm của hướng dẫn này, hạn mức miễn phí thường là đủ.

Nếu hạn mức không đủ:
1. Xem công cụ có tùy chọn trả phí không
2. Tối ưu Prompt của bạn, giảm bớt đối thoại không cần thiết
3. Chờ làm mới hạn mức tháng

## Điểm cốt yếu phần này

✓ **Công cụ đã cài đặt**: Máy tính của bạn có một AI IDE khả dụng

✓ **Giao diện đã quen thuộc**: Biết khu vực đối thoại, khu vực code, khu vực file, khu vực xem trước nằm ở đâu

✓ **Dự án đã tạo**: Có một thư mục dự án chuyên dụng, AI IDE đã mở thư mục này

✓ **Test đã thông qua**: Dùng "Hello, Vibe Coding!" xác minh công cụ hoạt động bình thường

::: tip Bước tiếp theo
Sau khi xác nhận công cụ khả dụng, chúng ta sẽ kiểm tra môi trường lần cuối.
:::

→ [4.1.3 Chuẩn bị công cụ và kiểm tra môi trường](./4.1.3-environment-check.md)
