---
title: "3.6.3 Chiến lược sửa chữa: Làm sao để AI sửa đúng"
---


![03-technique_3.6-debugging-ai_3.6.3-correction-strategies.png](../../../public/images/Basic/03-technique_3.6-debugging-ai_3.6.3-correction-strategies.png)
# 3.6.3 Chiến lược sửa chữa: Làm sao để AI sửa đúng

## Qua phần học này, bạn sẽ nắm vững

- Bốn chiến lược sửa chữa và bối cảnh áp dụng
- Quy trình làm việc của "phát triển định hướng bởi lỗi" (Error-Driven Development)
- Nhận biết tín hiệu "vòng xoáy chết chóc" và cắt lỗ kịp thời
- Có được một bộ mẫu Prompt sửa chữa có thể dùng ngay

## Bốn chiến lược sửa chữa

Trong phần 3.5.3, chúng ta đã học cách đưa ra phản hồi hiệu quả cho AI. Phần này tập trung vào chiến lược sửa chữa **sau khi vấn đề đã xảy ra** — khi AI đưa ra code có vấn đề, làm thế nào để hướng dẫn nó sửa cho đúng.

| Chiến lược | Bối cảnh áp dụng | Tư duy cốt lõi |
|-----|---------|---------|
| Chỉ ra chính xác vấn đề | Bạn biết cụ thể sai ở đâu | Đưa ra vị trí + Vấn đề + Kỳ vọng |
| Đưa ra ví dụ đúng | Định dạng hoặc cấu trúc không đúng | Cho xem "hình mẫu" đúng |
| Làm lại từng bước | Nhiệm vụ quá phức tạp dẫn đến sai sót | Tách nhỏ rồi hoàn thành từng cái |
| Đổi cách hỏi | Một cách hỏi thất bại nhiều lần | Thay đổi góc độ hoặc phương thức mô tả |

### Chiến lược 1: Chỉ ra chính xác vấn đề

**Bối cảnh áp dụng**: Bạn có thể định vị được cụ thể dòng code nào, hàm nào có vấn đề.

**Mẫu Prompt**:

```markdown
Code ở [vị trí] của bạn có vấn đề:

**Vấn đề**: [Mô tả vấn đề cụ thể]

**Hành vi hiện tại**: [Code hiện tại đang làm gì]

**Hành vi kỳ vọng**: [Code nên làm gì]

Hãy sửa vấn đề này, giữ nguyên các phần khác.
```

**Ví dụ**:

```markdown
Code ở dòng 15 hàm handleSubmit của bạn có vấn đề:

**Vấn đề**: Sử dụng localStorage, nhưng đây là dự án Server-Side Rendering (SSR)

**Hành vi hiện tại**: Gọi trực tiếp localStorage.setItem(), sẽ báo lỗi ở phía server

**Hành vi kỳ vọng**: Kiểm tra xem có phải môi trường trình duyệt không trước, hoặc sử dụng cookies

Hãy sửa vấn đề này, giữ nguyên các phần khác.
```

### Chiến lược 2: Đưa ra ví dụ đúng

**Bối cảnh áp dụng**: AI hiểu nhiệm vụ, nhưng định dạng, cấu trúc đầu ra không phù hợp yêu cầu của bạn.

**Mẫu Prompt**:

```markdown
Định dạng đầu ra không đúng.

**Định dạng hiện tại**:
[Cho xem định dạng AI đưa]

**Định dạng kỳ vọng**:
[Cho xem định dạng bạn muốn]

Hãy xuất lại theo định dạng kỳ vọng.
```

**Ví dụ**:

```markdown
Định dạng trả về của hàm không đúng.

**Định dạng hiện tại**:
Trả về dữ liệu trực tiếp, ví dụ `return userData`

**Định dạng kỳ vọng**:
Định dạng trả về thống nhất `{ success: boolean, data: any, error?: string }`
Khi thành công: `{ success: true, data: userData }`
Khi thất bại: `{ success: false, data: null, error: "Thông tin lỗi" }`

Hãy sửa giá trị trả về của tất cả hàm API theo định dạng kỳ vọng.
```

### Chiến lược 3: Làm lại từng bước

**Bối cảnh áp dụng**: Nhiệm vụ quá phức tạp, code AI xuất ra một lần có nhiều vấn đề, sửa vá không bằng làm lại.

**Mẫu Prompt**:

```markdown
Chúng ta làm chức năng này theo từng bước.

**Bước 1 (Hiện tại)**: [Chức năng cốt lõi nhỏ nhất]
Chỉ cần thực hiện chức năng này thôi, những cái khác thêm sau.

Sau khi hoàn thành tôi sẽ xác nhận, rồi chúng ta làm bước 2.
```

**Ví dụ**:

```markdown
Chúng ta làm chức năng xác thực người dùng theo từng bước.

**Bước 1 (Hiện tại)**: Chỉ thực hiện đăng ký người dùng
- Form email + mật khẩu
- Xác thực định dạng cơ bản
- Lưu vào cơ sở dữ liệu

Không cần: Đăng nhập, tìm lại mật khẩu, xác thực email, JWT

Sau khi hoàn thành tôi sẽ xác nhận, rồi chúng ta làm chức năng đăng nhập.
```

### Chiến lược 4: Đổi cách hỏi

**Bối cảnh áp dụng**: Cùng một vấn đề, dùng cùng một cách hỏi hai ba lần, AI vẫn đưa ra đáp án sai tương tự.

**Góc độ chuyển đổi có thể thử**:

| Cách hỏi cũ | Đổi cách hỏi |
|-------|---------|
| Đòi kết quả trực tiếp | Hỏi nguyên lý trước, rồi bảo AI thực hiện dựa trên nguyên lý |
| Dùng thuật ngữ kỹ thuật | Dùng ngôn ngữ đời thường mô tả nhu cầu |
| Mô tả "cần cái gì" | Mô tả "giải quyết vấn đề gì" |
| Hỏi từ góc độ chức năng | Mô tả bối cảnh từ góc độ người dùng |

**Ví dụ**:

```markdown
Cách hỏi cũ (thất bại nhiều lần):
"Viết một hàm debounce"

Đổi cách hỏi:
"Khi người dùng gõ phím trong ô tìm kiếm, tôi không muốn mỗi lần gõ một chữ lại gửi yêu cầu tìm kiếm,
mà muốn sau khi người dùng ngừng gõ 300ms mới gửi yêu cầu.
Hãy giúp tôi thực hiện logic kích hoạt trễ này."
```

## Phát triển định hướng bởi lỗi: Biến AI thành trợ lý debug

Khi bạn gặp lỗi, không cần tự mình phân tích — có thể ném trực tiếp thông tin lỗi cho AI, để nó giúp bạn định vị và sửa chữa.

### Quy trình làm việc

```
Phát hiện lỗi → Sao chép thông tin lỗi đầy đủ → Đưa cho AI phân tích → Áp dụng sửa chữa → Xác minh → Nếu có lỗi mới thì lặp lại
```

### Mẫu thao tác

```markdown
Khi chạy code gặp lỗi sau:

```
[Dán thông tin lỗi đầy đủ]
```

Code liên quan:
```[Ngôn ngữ]
[Dán đoạn code liên quan]
```

Hãy phân tích nguyên nhân lỗi, và đưa ra giải pháp sửa chữa.
```

### Ví dụ

```markdown
Khi chạy code gặp lỗi sau:

```
TypeError: Cannot read properties of undefined (reading 'map')
    at UserList (UserList.jsx:12:23)
```

Code liên quan:
```jsx
function UserList({ users }) {
  return (
    <ul>
      {users.map(user => <li key={user.id}>{user.name}</li>)}
    </ul>
  );
}
```

Hãy phân tích nguyên nhân lỗi, và đưa ra giải pháp sửa chữa.
```

**AI thường sẽ bảo bạn**:
- Nguyên nhân lỗi: `users` có thể là `undefined`
- Giải pháp sửa chữa: Thêm kiểm tra giá trị rỗng hoặc giá trị mặc định

## Cảnh giác với "Vòng xoáy chết chóc"

"Vòng xoáy chết chóc" nghĩa là: Bạn bảo AI sửa vấn đề, nó sửa một cái lại sinh ra hai vấn đề mới; bạn lại bảo nó sửa, vấn đề càng ngày càng nhiều, code càng ngày càng loạn.

### Tín hiệu nhận biết

- 🚨 Số vòng sửa đổi vượt quá 3 lần, vấn đề vẫn chưa giải quyết
- 🚨 Lượng code của AI càng sửa càng nhiều, càng sửa càng phức tạp
- 🚨 Sửa vấn đề A, chức năng B lại hỏng
- 🚨 AI bắt đầu "đi lòng vòng", đưa ra giải pháp tương tự như trước đó

### Chiến lược cắt lỗ

Một khi nhận thấy vòng xoáy chết chóc, hãy dứt khoát cắt lỗ:

**Lựa chọn 1: Quay lại làm lại từ đầu**
```markdown
Hướng đi này không ổn. Hãy quên những sửa đổi trước đó đi, chúng ta bắt đầu lại từ đầu.

[Mô tả lại nhu cầu rõ ràng, có thể cần tách thành nhiệm vụ nhỏ hơn]
```

**Lựa chọn 2: Giữ lại phần dùng được, tự sửa phần còn lại**
```markdown
Hãy sắp xếp lại phần code hiện tại có thể chạy bình thường cho tôi,
phần có vấn đề hãy dùng chú thích đánh dấu ra, tôi sẽ tự sửa.
```

**Lựa chọn 3: Thử công cụ AI khác**

Các model AI khác nhau có hiệu suất khác nhau trên các nhiệm vụ khác nhau. Nếu một công cụ cứ kẹt mãi, thử đổi cái khác có thể sẽ có bất ngờ.

## Kho mẫu Prompt sửa chữa

### Sửa lỗi Runtime (lỗi khi chạy)

```markdown
Đoạn code này báo lỗi:

Thông tin lỗi:
```
[Thông tin lỗi]
```

Code:
```[Ngôn ngữ]
[Code]
```

Hãy sửa lỗi này. Nếu cần thêm ngữ cảnh, hãy cho tôi biết cần thông tin gì.
```

### Sửa lỗi Logic

```markdown
Đoạn code này chạy được, nhưng kết quả không đúng:

**Đầu vào**: [Mô tả đầu vào]
**Đầu ra kỳ vọng**: [Mô tả kết quả kỳ vọng]
**Đầu ra thực tế**: [Mô tả kết quả thực tế]

Code:
```[Ngôn ngữ]
[Code]
```

Hãy tìm ra vấn đề logic và sửa lại.
```

### Sửa vấn đề Style

```markdown
Style không khớp với kỳ vọng:

**Hiệu quả kỳ vọng**: [Mô tả hoặc link ảnh chụp màn hình]
**Hiệu quả thực tế**: [Mô tả vấn đề hiện tại]

Code hiện tại:
```css
[Code style]
```

Hãy sửa style sao cho phù hợp với hiệu quả kỳ vọng.
```

### Yêu cầu AI tự kiểm tra

```markdown
Hãy kiểm tra code trên:

1. Có những trường hợp biên tiềm ẩn nào chưa được xử lý?
2. Có những chỗ nào có thể dẫn đến lỗi runtime?
3. Có những chỗ nào có thể tối ưu hóa?

Sau khi liệt kê vấn đề, hãy đưa ra code hoàn chỉnh sau khi sửa.
```

## Điểm cốt yếu phần này

✓ **Bốn chiến lược sửa chữa**: Chỉ ra chính xác → Đưa ra ví dụ → Làm lại từng bước → Đổi cách hỏi

✓ **Phát triển định hướng bởi lỗi**: Ném trực tiếp thông tin lỗi cho AI, để nó giúp bạn định vị và sửa chữa

✓ **Cảnh giác vòng xoáy chết chóc**: Sửa quá 3 vòng chưa xong, dứt khoát cắt lỗ làm lại

✓ **Kho mẫu Prompt**: Tích lũy các Prompt sửa chữa thường dùng, nâng cao hiệu suất sửa lỗi

Nhưng đôi khi, vấn đề không nằm ở chỗ sửa thế nào, mà là AI thực sự không làm được. Phần tiếp theo chúng ta sẽ xem chiến lược dự phòng hoàn chỉnh — phải làm gì khi AI thực sự "bó tay".
