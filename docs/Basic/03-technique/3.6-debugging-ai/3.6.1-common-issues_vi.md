---
title: "3.6.1 Những biểu hiện "không nghe lời" thường gặp của AI"
---


![03-technique_3.6-debugging-ai_3.6.1-common-issues.png](../../../public/images/Basic/03-technique_3.6-debugging-ai_3.6.1-common-issues.png)
# 3.6.1 Những biểu hiện "không nghe lời" thường gặp của AI

## Qua phần học này, bạn sẽ nắm vững

- Nhận biết sáu loại vấn đề phổ biến trong output của AI
- Hiểu bốn dạng ảo giác AI và tác hại của chúng
- Tìm hiểu về mối đe dọa bảo mật mới nổi "Slopsquatting" trong năm 2024-2025
- Có được một bảng tra cứu nhanh các kỹ thuật xác minh thực dụng

## Sáu loại vấn đề phổ biến

Biểu hiện "không nghe lời" của AI có vẻ muôn hình vạn trạng, nhưng quy lại chủ yếu có sáu loại:

| Loại | Triệu chứng | Ví dụ điển hình |
|-----|------|---------|
| **Hỏi một đằng trả lời một nẻo** | AI hiểu sai ý đồ của bạn | Bạn cần trang đăng nhập, nó đưa trang đăng ký |
| **Phát huy quá đà** | Làm những việc bạn không yêu cầu | Bạn cần đồng hồ đơn giản, nó đưa bản full tính năng có chuyển đổi múi giờ |
| **Ăn bớt công đoạn** | Làm thiếu những phần quan trọng | Không có xử lý lỗi, thiếu kiểm tra biên |
| **Tự mâu thuẫn** | Logic trước sau không nhất quán | Chú thích bảo "trả về mảng", code thực tế trả về object |
| **Ảo giác** | Bịa đặt nội dung không tồn tại | Giới thiệu thư viện không có thật, gọi API không tồn tại |
| **Vòng lặp chết** | Lặp đi lặp lại cùng một lỗi | Bạn chỉ ra lỗi, nó sửa, lần sau lại sửa quay về cái cũ |

### Loại 1: Hỏi một đằng trả lời một nẻo

**Bối cảnh**: Bạn bảo AI làm một "chức năng đăng nhập người dùng", nó lại đưa cho bạn trọn bộ "đăng ký người dùng + đăng nhập + tìm lại mật khẩu".

**Tại sao lại thế**: AI dựa trên lượng lớn dữ liệu huấn luyện để khớp mẫu, khi nó thấy từ "đăng nhập", sẽ liên tưởng đến hệ thống xác thực người dùng phổ biến, rồi "tốt bụng" đưa cho bạn trọn bộ giải pháp.

**Tín hiệu nhận biết**:
- Nội dung output nhiều hơn bạn cần rất nhiều
- Bao gồm các module chức năng bạn không đề cập
- Khi giải thích AI nói "Tôi giả định bạn còn cần..."

### Loại 2: Phát huy quá đà

**Bối cảnh**: Bạn chỉ cần một component đơn giản hiển thị thời gian hiện tại, AI lại thêm vào chọn múi giờ, chuyển đổi 12/24 giờ, định dạng tùy chỉnh, v.v.

**Tại sao lại thế**: AI đã thấy quá nhiều giải pháp "hoàn chỉnh", có xu hướng đưa ra cách thực hiện "toàn diện", ngay cả khi bạn chỉ cần phiên bản đơn giản nhất.

**Tín hiệu nhận biết**:
- Lượng code vượt xa dự kiến
- Import các thư viện bên thứ ba bạn không biết
- Bao gồm lượng lớn tùy chọn cấu hình bạn không cần

### Loại 3: Ăn bớt công đoạn

**Bối cảnh**: Code AI đưa chạy được, nhưng không xử lý các tình huống bất thường như "người dùng nhập rỗng", "yêu cầu mạng thất bại".

**Tại sao lại thế**: Để code trông gọn gàng, hoặc do bạn không yêu cầu rõ ràng, AI đã lược bỏ code phòng vệ.

**Tín hiệu nhận biết**:
- Trong code không có try-catch hoặc xử lý lỗi
- Không có xác thực nhập liệu
- "Happy Path" (Đường đi lý tưởng) chạy được, nhưng trường hợp biên sẽ bị lỗi

### Loại 4: Tự mâu thuẫn

**Bối cảnh**: AI nói "hàm này trả về danh sách người dùng", nhưng code thực tế trả về một object người dùng đơn lẻ.

**Tại sao lại thế**: Quá trình tạo chú thích và tạo code của AI tương đối độc lập, đôi khi sẽ xảy ra tình trạng không đồng bộ.

**Tín hiệu nhận biết**:
- Chú thích không khớp với hành vi code
- Tên hàm ám chỉ chức năng không nhất quán với thực hiện thực tế
- Giải pháp nói ở trước và code ở sau không khớp nhau

### Loại 5: Ảo giác (Trọng điểm)

Đây là loại vấn đề nguy hiểm nhất, sẽ được trình bày chi tiết ở dưới.

### Loại 6: Vòng lặp chết

**Bối cảnh**: Bạn chỉ ra code có vấn đề, AI sửa xong, vòng sau lại sửa quay về cách viết cũ.

**Tại sao lại thế**:
- Ngữ cảnh hội thoại quá dài, AI "quên" mất những sửa đổi trước đó
- Chỉ thị sửa đổi của bạn không đủ rõ ràng
- Bản thân nhiệm vụ vượt quá khả năng của AI

**Tín hiệu nhận biết**:
- Lỗi giống nhau xuất hiện lặp lại
- AI sửa càng ngày càng "loanh quanh"
- Sửa một lỗi sinh ra hai lỗi mới

## Trọng điểm: Nhận biết và ứng phó với ảo giác AI

Ảo giác AI (Hallucination) là chỉ việc AI tạo ra nội dung **trông có vẻ đúng nhưng thực tế là sai**. Trong bối cảnh lập trình, điều này có thể dẫn đến code không chạy được, thậm chí gây ra lỗ hổng bảo mật.

### Bốn loại ảo giác code

| Loại | Biểu hiện | Mức độ nguy hại | Ví dụ |
|------|------|---------|------|
| **Ảo giác gói (Package)** | Giới thiệu gói npm/pip không tồn tại | 🔴 Cao (Rủi ro bảo mật) | `npm install super-easy-utils` (Gói không tồn tại) |
| **Ảo giác API** | Bịa đặt hàm hoặc phương thức không tồn tại | 🟡 Trung bình | Gọi `array.findLast()` không tồn tại trong JS bản cũ |
| **Ảo giác logic** | Logic code không khớp với mô tả | 🟡 Trung bình | Nói "trả về mảng đã khử trùng lặp", thực tế không khử trùng lặp |
| **Ảo giác phiên bản** | Sử dụng cú pháp hoặc API đã lỗi thời | 🟡 Trung bình | Sử dụng vòng đời class component phiên bản cũ của React |

### Ảo giác gói: Loại ảo giác nguy hiểm nhất

Tại sao ảo giác gói lại có mức độ nguy hại cao nhất? Vì nó liên quan đến **an toàn chuỗi cung ứng phần mềm**.

**Trường hợp thực tế**: Nghiên cứu bảo mật năm 2024-2025 đã phát hiện một phương thức tấn công gọi là "Slopsquatting":

1. Các nhà nghiên cứu đã thử nghiệm 16 model AI chủ lưu, phát hiện khoảng **20% các gói chúng giới thiệu là không tồn tại**
2. Đáng lo ngại hơn là, **43% tên gói ảo giác sẽ xuất hiện lặp lại trong nhiều lần hỏi**
3. Kẻ tấn công có thể đăng ký các tên gói mà AI thường xuyên "ảo giác" ra này, cấy vào mã độc
4. Khi lập trình viên cài đặt các gói này theo gợi ý của AI, họ sẽ dính bẫy

**Chuỗi tấn công**:
```
AI ảo giác ra tên gói → Kẻ tấn công đăng ký tên gói đó → Cấy mã độc → Lập trình viên cài đặt theo gợi ý AI → Hệ thống bị xâm nhập
```

**Mối đe dọa thực tế**: Tháng 10 năm 2025, công ty bảo mật đã phát hiện hoạt động độc hại tên là "PhantomRaven", lợi dụng kỹ thuật này lây nhiễm 126 gói npm, đánh cắp thông tin nhạy cảm như npm tokens, GitHub credentials của lập trình viên.

### Ảo giác API: Trông có vẻ đúng nhưng không chạy được

**Bối cảnh điển hình**:

```javascript
// Code AI tạo ra
const lastItem = myArray.findLast(item => item.active);
```

Vấn đề: `findLast()` là phương thức được giới thiệu trong ES2023, không khả dụng trong trình duyệt cũ hoặc Node.js bản cũ. AI có thể nhầm lẫn API của các phiên bản khác nhau.

**Các mô hình ảo giác API thường gặp**:
- Nhầm lẫn API của các thư viện khác nhau (xem phương thức của Lodash là phương thức native)
- Sử dụng API đã lỗi thời
- Bịa đặt tùy chọn cấu hình không tồn tại

### Ảo giác logic: Code chạy được nhưng kết quả sai

**Bối cảnh điển hình**:

Bạn bảo AI viết một "hàm khử trùng lặp", nó đưa cho bạn:

```javascript
function removeDuplicates(arr) {
  // Loại bỏ các phần tử trùng lặp trong mảng
  return arr.filter((item, index) => arr.indexOf(item) === index);
}
```

Code trông có vẻ không vấn đề, chú thích cũng đúng. Nhưng nếu trong mảng có chứa object, phương thức này sẽ mất hiệu lực, vì object so sánh tham chiếu chứ không phải giá trị.

### Ảo giác phiên bản: Dùng cách viết "lỗi thời"

**Bối cảnh điển hình**:

Bạn bảo AI dùng React viết một component, nó đưa cho bạn cách viết class component:

```javascript
class MyComponent extends React.Component {
  componentWillMount() {
    // ...
  }
}
```

Vấn đề: `componentWillMount` đã bị loại bỏ sau React 16.3, trong React 18 sẽ sinh ra cảnh báo.

## Bảng tra cứu nhanh kỹ thuật xác minh

Đối mặt với code do AI tạo ra, dùng danh sách này để xác minh nhanh:

### Xác minh gói có tồn tại không

```bash
# npm package
npm search <tên_gói>
# Hoặc truy cập trực tiếp https://www.npmjs.com/package/<tên_gói>

# Python package
pip index versions <tên_gói>
# Hoặc truy cập trực tiếp https://pypi.org/project/<tên_gói>
```

### Xác minh API có chính xác không

1. **Tra cứu tài liệu chính thức**: Đừng chỉ tin lời giải thích của AI
2. **Kiểm tra tính tương thích**: Sử dụng [caniuse.com](https://caniuse.com) (Frontend) hoặc xem ghi chú phiên bản của thư viện
3. **Test phạm vi nhỏ**: Chạy thử trong môi trường độc lập trước, xác nhận dùng được

### Yêu cầu AI cung cấp nguồn

Khi AI giới thiệu thư viện hoặc API bạn không quen thuộc, hãy truy vấn:

```
Vui lòng cung cấp link tài liệu chính thức của thư viện này.
```

```
API này được giới thiệu ở phiên bản nào? Vui lòng cung cấp nguồn tham khảo.
```

Nếu AI không thể đưa ra link hợp lệ, hoặc link mở ra là 404, thì rất có thể là ảo giác.

### Xác minh tính chính xác của logic

1. **Test bằng trường hợp biên**: Mảng rỗng, chuỗi rỗng, null, số cực lớn
2. **Test bằng phản ví dụ**: Cố ý nhập dữ liệu "nên thất bại", xem có xử lý đúng không
3. **Để AI tự kiểm tra**:
   ```
   Hãy kiểm tra code trên, liệt kê các trường hợp biên và ngoại lệ có thể xảy ra, và xem code hiện tại đã xử lý đúng chúng chưa.
   ```

## Điểm cốt yếu phần này

✓ **Sáu loại vấn đề**: Hỏi một đằng trả lời một nẻo, phát huy quá đà, ăn bớt công đoạn, tự mâu thuẫn, ảo giác, vòng lặp chết

✓ **Bốn loại ảo giác code**: Ảo giác gói (nguy hiểm nhất), ảo giác API, ảo giác logic, ảo giác phiên bản

✓ **Mối đe dọa Slopsquatting**: Kẻ tấn công lợi dụng tên gói do AI ảo giác để tấn công chuỗi cung ứng, 20% gói AI giới thiệu có thể không tồn tại

✓ **Thói quen xác minh**: Với gói và API do AI giới thiệu, xác minh trước rồi mới dùng; với logic code, test bằng trường hợp biên

Nhận biết vấn đề chỉ là bước đầu tiên. Phần tiếp theo, chúng ta sẽ học cách nhanh chóng định vị gốc rễ vấn đề.
